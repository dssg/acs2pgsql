ACS_PROFILE:=acs_profile
%include $[ACS_PROFILE]

psql()
	psql -v ON_ERROR_STOP=1 -f $[INPUT] && touch $[OUTPUT]

psql/acs/build_directories_schemas <- build_directories_schemas.sh
	bash $INPUT acs && touch $OUTPUT


%include input/Drakefile_acs
